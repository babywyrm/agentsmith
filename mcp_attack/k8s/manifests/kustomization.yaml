apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-audit

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - job.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize

images:
  - name: mcp-audit
    newTag: latest
