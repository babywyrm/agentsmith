# Agent Smith MCP Server Dependencies
# Install with: pip install -r mcp_server/requirements.txt

# MCP SDK with SSE + Streamable HTTP transports
mcp[cli]>=1.20.0

# ASGI server
uvicorn>=0.30.0

# ASGI framework (used by MCP transports)
starlette>=0.40.0

# Async support
anyio>=4.0.0

# HTTP client (used by test client)
httpx>=0.27.0

# Also requires Agent Smith core dependencies (../requirements.txt)
