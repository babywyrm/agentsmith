# Agent Smith Dependencies (pip fallback)
# Source of truth: pyproject.toml
# Preferred install: uv sync --all-extras
# Fallback install:  pip install -e ".[mcp-server,dev]"

anthropic>=0.18.0
rich>=13.0.0
typing-inspection>=0.1.0
tqdm>=4.65.0

